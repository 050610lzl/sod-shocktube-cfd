# ============================================================
# 一维Sod激波管CFD项目 - Python依赖清单
# 依据: Build文档 §3.2
# ============================================================

# --- 核心依赖 ---
numpy>=1.21.0,<2.0.0        # 数值计算 (上限<2.0.0避免breaking changes)
matplotlib>=3.5.0           # 可视化绘图
pyyaml>=6.0                 # YAML配置文件解析
scipy>=1.7.0                # 科学计算 (brentq求根等)

# --- 测试依赖 ---
pytest>=7.0.0               # 单元测试框架 (54项测试)

# --- 开发工具 (可选) ---
flake8>=5.0.0               # 代码风格检查
